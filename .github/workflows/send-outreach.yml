name: 📬 Send Outreach Emails
# Manually triggered. Sends via unified_emailer — one log, no duplicates.
on:
  workflow_dispatch:

jobs:
  outreach:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Send outreach (unified, brain-gated)
        env:
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: EMAIL_MODE=hello python mycelium/unified_emailer.py
      - name: Commit sent log
        run: |
          git config user.email "solarpunk.mycelium@gmail.com"
          git config user.name "SolarPunk Mycelium"
          git add data/ || true
          git diff --staged --quiet || git commit -m "auto: outreach emails sent $(date -u +%Y-%m-%d)"
          git push || true
