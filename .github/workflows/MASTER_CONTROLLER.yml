name: 🌸 MASTER CONTROLLER — Full Passive Income + Job Agent

on:
  schedule:
    - cron: '0 6 * * *'    # 6am UTC daily — FULL CYCLE
    - cron: '0 18 * * *'   # 6pm UTC daily — EVENING CYCLE
  workflow_dispatch:
    inputs:
      mode:
        description: 'full_cycle | revenue_only | jobs_only | health_check'
        required: false
        default: 'full_cycle'

jobs:
  master_control:
    name: 🌸 SolarPunk Master Controller
    runs-on: ubuntu-latest
    timeout-minutes: 55
    permissions:
      contents: write
      actions: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Configure git
        run: |
          git config user.name 'SolarPunk-MasterController'
          git config user.email 'solarpunk@meeko-nerve-center'
          mkdir -p data content public

      # ══════════════════════════════════════════════
      # PHASE 0 — Human Directives (YOU steer from Notion)
      # ══════════════════════════════════════════════
      - name: Phase 0 — Read Human Directives from Notion
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python mycelium/notion_directives_reader.py
        continue-on-error: true

      # ══════════════════════════════════════════════
      # PHASE 1 — Self-heal
      # ══════════════════════════════════════════════
      - name: Phase 1 — Self-Healer
        env:
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python mycelium/self_healer_v2.py
        continue-on-error: true

      # ══════════════════════════════════════════════
      # PHASE 1.5 — 3D Brain (directive-informed synthesis)
      # ══════════════════════════════════════════════
      - name: Phase 1.5 — 3D Brain
        env:
          GUMROAD_SECRET: ${{ secrets.GUMROAD_SECRET }}
          GUMROAD_ID: ${{ secrets.GUMROAD_ID }}
          GUMROAD_NAME: ${{ secrets.GUMROAD_NAME }}
          MASTODON_TOKEN: ${{ secrets.MASTODON_TOKEN }}
          MASTODON_BASE_URL: ${{ secrets.MASTODON_BASE_URL }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python mycelium/three_d_brain.py
        continue-on-error: true

      # ══════════════════════════════════════════════
      # PHASE 2 — Long-Term Memory (all 3 sources)
      # ══════════════════════════════════════════════
      - name: Phase 2 — Long-Term Memory (data + Notion + HF)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
        run: python mycelium/long_term_memory.py
        continue-on-error: true

      - name: Phase 3 — Workflow Health Monitor
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python mycelium/master_controller.py
        continue-on-error: true

      - name: Phase 4a — World Intelligence
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python mycelium/world_intelligence.py
        continue-on-error: true

      - name: Phase 4b — Congress Watcher
        env:
          QUIVER_API_KEY: ${{ secrets.QUIVER_API_KEY }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python mycelium/congress_watcher.py
        continue-on-error: true

      - name: Phase 4c — Crypto Signals
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python mycelium/crypto_signal_engine.py
        continue-on-error: true

      - name: Phase 5 — Investment Intelligence
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python mycelium/investment_intelligence.py
        continue-on-error: true

      - name: Phase 6a — Art Generation
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_USERNAME: ${{ secrets.HF_USERNAME }}
        run: python mycelium/art_cause_generator.py
        continue-on-error: true

      - name: Phase 6b — Social Posting (directive + memory aware)
        env:
          MASTODON_TOKEN: ${{ secrets.MASTODON_TOKEN }}
          MASTODON_BASE_URL: ${{ secrets.MASTODON_BASE_URL }}
          MASTODON_URL: ${{ secrets.MASTODON_URL }}
          BLUESKY_HANDLE: ${{ secrets.BLUESKY_HANDLE }}
          BLUESKY_PASSWORD: ${{ secrets.BLUESKY_PASSWORD }}
          BLUESKY_APP_PASSWORD: ${{ secrets.BLUESKY_APP_PASSWORD }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python mycelium/social_poster.py
        continue-on-error: true

      - name: Phase 7 — Network Spreader
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MASTODON_TOKEN: ${{ secrets.MASTODON_TOKEN }}
          MASTODON_BASE_URL: ${{ secrets.MASTODON_BASE_URL }}
        run: python mycelium/network_spreader.py
        continue-on-error: true

      - name: Phase 8 — Grant Intelligence (memory-informed v3)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python mycelium/grant_intelligence.py
        continue-on-error: true

      # ══════════════════════════════════════════════
      # PHASE 9 — Revenue: ALL streams simultaneously
      # ══════════════════════════════════════════════
      - name: Phase 9a — Gumroad Revenue Check
        env:
          GUMROAD_NAME: ${{ secrets.GUMROAD_NAME }}
          GUMROAD_ID: ${{ secrets.GUMROAD_ID }}
          GUMROAD_SECRET: ${{ secrets.GUMROAD_SECRET }}
        run: python mycelium/gumroad_tracker.py
        continue-on-error: true

      - name: Phase 9b — Etsy Bridge (NEW — 90M buyers)
        env:
          ETSY_API_KEY: ${{ secrets.ETSY_API_KEY }}
          ETSY_SHOP_ID: ${{ secrets.ETSY_SHOP_ID }}
          ETSY_ACCESS_TOKEN: ${{ secrets.ETSY_ACCESS_TOKEN }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python mycelium/etsy_bridge.py
        continue-on-error: true

      - name: Phase 9c — Revenue Router (route all income automatically)
        env:
          PAYPAL_CLIENT_ID: ${{ secrets.PAYPAL_CLIENT_ID }}
          PAYPAL_CLIENT_SECRET: ${{ secrets.PAYPAL_CLIENT_SECRET }}
          PHANTOM_WALLET_ADDRESS: ${{ secrets.PHANTOM_WALLET_ADDRESS }}
          HUMAN_PAYPAL_EMAIL: ${{ secrets.HUMAN_PAYPAL_EMAIL }}
          PCRF_PAYPAL_EMAIL: ${{ secrets.PCRF_PAYPAL_EMAIL }}
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
        run: python mycelium/revenue_router.py
        continue-on-error: true

      # ══════════════════════════════════════════════
      # PHASE 10 — Job Agent (system applies for work)
      # ══════════════════════════════════════════════
      - name: Phase 10 — Job Agent (NEW — system IS the employee)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          UPWORK_API_KEY: ${{ secrets.UPWORK_API_KEY }}
          HUMAN_FULL_NAME: ${{ secrets.HUMAN_FULL_NAME }}
          HUMAN_EMAIL: ${{ secrets.HUMAN_EMAIL }}
          HUMAN_LOCATION: ${{ secrets.HUMAN_LOCATION }}
          HUMAN_TIMEZONE: ${{ secrets.HUMAN_TIMEZONE }}
        run: python mycelium/job_agent.py
        continue-on-error: true

      - name: Phase 11 — Perpetual Builder
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python mycelium/perpetual_builder.py
        continue-on-error: true

      - name: Phase 12 — Loop Closer
        run: python mycelium/loop_closer.py
        continue-on-error: true

      - name: Phase 13 — Dashboard Generator
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python mycelium/dashboard_data_generator.py
        continue-on-error: true

      - name: Phase 14 — Deep Diagnostic
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python mycelium/deep_diagnostic.py
        continue-on-error: true

      # ══════════════════════════════════════════════
      # PHASE 15 — HF Dataset Logger (everything becomes training data)
      # ══════════════════════════════════════════════
      - name: Phase 15 — HF Dataset Logger (training data flywheel)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_USERNAME: ${{ secrets.HF_USERNAME }}
        run: python mycelium/hf_dataset_logger.py
        continue-on-error: true

      # ══════════════════════════════════════════════
      # PHASE 16 — Notion Sync (push everything to command center)
      # ══════════════════════════════════════════════
      - name: Phase 16 — Notion Sync
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          GUMROAD_SECRET: ${{ secrets.GUMROAD_SECRET }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python mycelium/notion_bridge.py
        continue-on-error: true

      - name: Email Gateway (inbound only)
        env:
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python mycelium/email_gateway.py
        continue-on-error: true

      - name: Autonomous Git Commit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git add -A
          git diff --cached --quiet && echo 'Nothing to commit' && exit 0
          git commit -m "🌸 cycle [$(date +%Y-%m-%d\ %H:%M)] — revenue+jobs+grants+content"
          git pull --rebase origin main || true
          git push origin main || git push origin main --force-with-lease
        continue-on-error: true
