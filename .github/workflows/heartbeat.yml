name: 💓 Daily Heartbeat
# 🚨 SCHEDULES DISABLED — Email system rebuild in progress
# Safe to re-enable after email_gateway.py confirmed fixed

on:
  workflow_dispatch:

jobs:
  heartbeat:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Space Bridge
        run: python mycelium/space_bridge.py
        continue-on-error: true
        env:
          NASA_API_KEY: ${{ secrets.NASA_API_KEY }}
      - name: Wiring Hub
        run: python mycelium/wiring_hub.py
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NASA_API_KEY: ${{ secrets.NASA_API_KEY }}
      - name: Update System State
        run: python mycelium/update_state.py
        continue-on-error: true
      - name: Generate Status Page
        run: python mycelium/gen_status.py
        continue-on-error: true
      - name: Commit
        run: |
          git config user.name "Meeko Heartbeat"
          git config user.email "meekotharaccoon@gmail.com"
          git add data/ 2>/dev/null || true
          git diff --staged --quiet || git commit -m "💓 heartbeat: $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push origin main || true
        continue-on-error: true
