name: 🌱 Evolution Engine
# 🚨 SCHEDULES DISABLED — Email system rebuild in progress

on:
  workflow_dispatch:

jobs:
  evolve:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run evolution
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python mycelium/perpetual_builder.py
        continue-on-error: true
      - name: Commit
        run: |
          git config user.name 'evolution-engine'
          git config user.email 'evolve@meeko-nerve-center'
          git add -A
          git diff --cached --quiet || git commit -m "🌱 evolved [$(date -u +%Y-%m-%d)]"
          git push origin main || true
        continue-on-error: true
