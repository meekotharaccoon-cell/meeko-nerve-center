name: 🧠 Orchestrator — Full Cycle

on:
  workflow_dispatch:

jobs:
  orchestrate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Run Full Cycle
        run: python mycelium/ORCHESTRATOR.py
        env:
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          NASA_API_KEY: ${{ secrets.NASA_API_KEY }}

      - name: Commit state
        run: |
          git config user.name "Meeko Mycelium"
          git config user.email "meekotharaccoon@gmail.com"
          git add data/
          git diff --staged --quiet || git commit -m "🧠 orchestrator cycle $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push origin main
        continue-on-error: true
